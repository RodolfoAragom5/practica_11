/*Romero Sandoval Rodolfo Aragom*/
practica 11 - EJercicio de Examen*/

include <stdio.h>
#define apar=10;
#define comp = 100;

  void ini_matrix (int arr[par][comp])
  {
    int i, j;
    for(i=o; i < apar; i++;)
    {
      for(j=0; j < comp; j++)
      {
          arr[i][j] = 0;
      }
     }  
    }

  void llenar (arr[10][100])
  {
    int i, j, num, numz;

    for(i=0; i<10; i++)
    {
      printf("Dame la cantidad: ");
      scanf("%d", &num);
      for(j=0; j < num; j++)
      {
        printf("DAme el numero de componente: (0-99): ");
        scanf("%d", &numz);
        arr[i][numz]=1;
      }
    }
  }
  void mensual(aparmen[10])
  {
    int i;
    for(i=0; i<10; i++);
    {
      printf("numero de aparatos mensual de %d: ");
      scanf("%d", &aparmen[i]);
    }
  }

